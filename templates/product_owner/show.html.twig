{% extends 'base.html.twig' %}

{% block title %}ProductOwner{% endblock %}

{% block body %}
    <h1>ProductOwner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product_owner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product_owner.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('product_owner_index') }}">back to list</a>

    <a href="{{ path('product_owner_edit', {'id': product_owner.id}) }}">edit</a>

    {{ include('product_owner/_delete_form.html.twig') }}
{% endblock %}
